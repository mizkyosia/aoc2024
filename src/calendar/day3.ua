# Experimental!
# Parse ← |1.2 ∩⍆⊡0⤙⊡1∵⋕⊏1_2⍉regex"(\\d+)   (\\d+)"
# &fras "../inputs/day1.txt"

# ---------- PARTIE 1
"xmul(2,4)&mul[3,7]!^don't()_mul(5,5)+mul(32,64](mul(11,8)undo()?mul(8,5))"
⬚"0"regex"(?:don't|do|mul\\(([0-9]{1,3}),([0-9]{1,3})\\))"
⊡0⍉.
:
⍉⊏1_2⍉
⍉⋕
/+./×

# ---------- PARTIE 2
◌
:
¯⦷{"don't"}.
⦷{"do"}:
+
?

⊜□≠, ¯1
.,⊏0.
⍚(↘⊗1)
↘1
◌:⊂:
/◇⊂
